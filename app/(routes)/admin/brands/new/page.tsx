import React from "react";
import AdminPageTitle from "@/app/(private)/_components/Headings/AdminPageTitle";

type Props = {};

const NewBrandPage = (props: Props) => {
    return (
        <div>
            <AdminPageTitle titleText={"New Brand"} />
        </div>
    );
};

export default NewBrandPage;
