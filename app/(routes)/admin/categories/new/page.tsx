import React from "react";
import AdminPageTitle from "@/app/(private)/_components/Headings/AdminPageTitle";

type Props = {};

const NewCategoryPage = (props: Props) => {
    return (
        <div>
            <AdminPageTitle titleText={"New Category"} />
        </div>
    );
};

export default NewCategoryPage;
